/** Header para carrito de invitado (crear con POST /cart/guest-session). */
export const GUEST_SESSION_HEADER = 'x-guest-session-id';

/** Header para consultar orden de invitado o crear preferencia de pago sin JWT. */
export const GUEST_ORDER_TOKEN_HEADER = 'x-guest-order-token';
